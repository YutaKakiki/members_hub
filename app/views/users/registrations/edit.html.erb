<div class="mb-6 ">
  <h1 class="text-2xl font-bold">登録情報を編集</h1>
</div>

<div class='flex flex-wrap  mb-6'>
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, class: "w-full xl:w-2/5" }, data: { turbo: false }) do |f| %>

    <%= render "shared/error_messages", resource: resource %>

    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <div class="mb-4 text-sm text-gray-600">
       本人確認がまだ完了していません: <%= resource.unconfirmed_email %>
      </div>
    <% end %>

    <div class="mb-4">
      <%= f.label :name, class: "block text-gray-700 text-sm font-medium mb-1" %>
      <%= f.text_field :name, class: "mt-1 block w-full border border-gray-300 rounded-md shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm" %>
    </div>

    <div class="mb-4">
      <%= f.label :email, class: "block text-gray-700 text-sm font-medium mb-1" %>
      <%= f.email_field :email, class: "mt-1 block w-full border border-gray-300 rounded-md shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm" %>
      <div class="text-xs  text-gray-500 mt-3">
        ※LINEアカウントでログインしている方は、メールアドレスが参照できなかった際にメールアドレスを自動生成しています。適宜変更をお願いします。
      </div>
    </div>

    <div class="actions mb-6">
      <%= f.submit "更新", class: "inline-flex rounded-md bg-blue-500 py-2 px-4 mt-10 text-white duration-100 ease-in-out hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2" %>
    </div>
  <% end %>
</div>
