
<div class="flex justify-between">
    <h1 class="text-2xl font-bold mb-6">メンバーを閲覧</h1>
    <%= render "shared/simple_team_card",size:"small" %>
</div>

<%= render 'teams/members/search_form' %>

<%=turbo_frame_tag "team-members" do%>
    <div class="grid grid-cols-2 grid-flow-row gap-5">
        <%= render partial: 'teams/members/member',collection: @members %> 
    </div>
    <div class="my-20">
        <%=paginate(@members)%>
    </div>
<% end %>

